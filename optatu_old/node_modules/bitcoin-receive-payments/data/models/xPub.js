var mongoose = require('mongoose')

module.exports.init = function() {
  var xpubSchema = new mongoose.Schema({
    xpub: { type: String, unique: true },
    // apikey: { type: String, unique: true },
  });

  var xPub = mongoose.model('xPub', xpubSchema);
};